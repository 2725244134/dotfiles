# 任务完成后的检查
- 核对改动的 YAML/Lua 是否缩进正确、键名一致；若有词库改动，确认列顺序未破坏。
- 重新部署并重启输入法：执行 `rime_deployer` 或通过前端“重新部署”，必要时 `fcitx5-remote -r`；确认部署无错误提示。
- 本地验证：切换到相关方案测试输入（含粘贴/快捷键等受影响功能），确保候选、符号、快捷键符合预期。
- 如有疑问，标注需要用户确认的行为或可能的副作用。